/target
TODO-LIST.md

# Tool-specific agent pointers (thin @AGENTS.md imports, never committed)
CLAUDE.md
GEMINI.md
.cursor/
.aider*
.windsurfrules

# Private reference material, never committed - see AGENTS.md
/.nocommit

# The demo rig's scratch: the fake machine stage.sh builds, rebuilt from
# demo/fixtures/ every time. Only the rig itself is committed.
/demo/home
